events {
    worker_connections 1024;
}

http {
    upstream backend_rr {
        # Default NGINX behavior is round-robin
        server upstream-s0:8000;
        server upstream-s1:8000;
        server upstream-s2:8000;
        server upstream-s3:8000;
        server upstream-s4:8000;
        server upstream-s5:8000;
    }

    server {
        listen 80;

        location /api/work {
            proxy_pass http://backend_rr/api/work;
            proxy_connect_timeout 5s;
            proxy_read_timeout 10s;
        }

        location /admin/ {
            proxy_pass http://backend_rr;
        }

        location /health {
            return 200 '{"status":"ok","balancer":"nginx-rr"}';
            add_header Content-Type application/json;
        }
    }
}
